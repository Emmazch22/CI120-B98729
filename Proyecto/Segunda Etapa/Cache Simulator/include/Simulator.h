#include <iostream>
#include <unistd.h>
#include <string.h>

class Simulator {
    private:

    public:
        Simulator();
        Simulator(size_t, size_t, size_t, size_t, size_t, size_t, std::string);
        ~Simulator();
};